# Web
PORT=4000
# Leave empty so the browser calls the same origin; webpack-dev-server proxies to the API.
# Set to http://localhost:3000 only when opening the UI on the same machine without the proxy.
API_HOST=

# reCAPTCHA v2 - Obtén tus claves en https://www.google.com/recaptcha/admin
# Para desarrollo puedes usar las claves de prueba de Google (siempre pasan)
RECAPTCHA_SITE_KEY=6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI
